# getent

## What it does
Fetches entries from system databases like users and groups.

## Basic syntax
```bash
getent passwd user
getent group group
```

## DevOps use cases
- Checking if user or group exists
