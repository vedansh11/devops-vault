# git commit

## What it does
Creates a snapshot of staged changes.

## Basic syntax
```bash
git commit -m "message"
```

## Important flags
- -m → commit message
- -a → stage tracked files

## DevOps use cases
- Tracking code and infra changes
